# 感恩之旅网页 - 公共服务器部署说明

## 🎉 项目概述

这是一个感恩之旅相册网站，支持移动端和电脑端访问，现在可以部署到公共服务器上供所有人访问。

## 🚀 快速开始

### 方式一：一键部署（推荐）

```bash
# 给脚本执行权限
chmod +x deploy.sh

# 运行一键部署脚本
./deploy.sh
```

### 方式二：手动部署

#### 1. 安装依赖
```bash
# 复制服务器配置
cp package-server.json package.json

# 安装依赖
npm install
```

#### 2. 启动服务
```bash
# 直接启动
node server.js

# 或使用PM2（推荐生产环境）
npm install -g pm2
pm2 start server.js --name "gratitude-journey"
pm2 save
pm2 startup
```

#### 3. Docker部署
```bash
# 构建镜像
docker build -t gratitude-journey .

# 运行容器
docker run -d --name gratitude-journey -p 3000:3000 gratitude-journey

# 或使用docker-compose
docker-compose up -d
```

## ☁️ 云平台部署指南

### 1. 腾讯云部署详细步骤

#### 方法A：云服务器部署（推荐新手）

**步骤1：购买云服务器**
1. 登录腾讯云控制台：https://console.cloud.tencent.com/cvm
2. 选择"云服务器" → "新建实例"
3. 配置推荐：
   - 地域：选择离您用户最近的地域
   - 实例：轻量应用服务器（性价比高）
   - 镜像：Ubuntu 20.04 LTS
   - 规格：1核2G（约50元/月）
   - 系统盘：50GB SSD

**步骤2：连接服务器**
```bash
# 使用SSH连接服务器
ssh root@您的服务器IP

# 创建非root用户（安全考虑）
adduser gratitude
usermod -aG sudo gratitude
su - gratitude
```

**步骤3：上传项目文件**
```bash
# 在服务器上安装必要的软件
sudo apt update
sudo apt install -y git nodejs npm

# 克隆项目或上传文件
# 方法1：使用Git克隆
git clone 您的项目仓库地址
cd project_11

# 方法2：使用SCP上传（从本地）
# 在本地终端执行：
scp -r d:/下载/project_11/* root@您的服务器IP:/home/gratitude/project_11/
```

**步骤4：一键部署**
```bash
cd project_11
chmod +x deploy.sh
./deploy.sh
```

#### 方法B：腾讯云容器服务

1. **创建镜像仓库**
   - 进入容器镜像服务 TCR
   - 创建命名空间和镜像仓库

2. **构建并推送镜像**
```bash
# 登录镜像仓库
sudo docker login --username=您的账号 ccr.ccs.tencentyun.com

# 构建镜像
docker build -t ccr.ccs.tencentyun.com/您的命名空间/gratitude-journey:latest .

# 推送镜像
docker push ccr.ccs.tencentyun.com/您的命名空间/gratitude-journey:latest
```

3. **部署到容器服务**
   - 在容器服务中创建集群
   - 创建工作负载，选择刚才推送的镜像
   - 配置服务端口3000

### 2. 阿里云部署详细步骤

#### 方法A：ECS云服务器部署

**步骤1：购买ECS实例**
1. 登录阿里云控制台：https://ecs.console.aliyun.com
2. 选择"实例" → "创建实例"
3. 配置推荐：
   - 地域：华东1（杭州）或华北2（北京）
   - 实例规格：ecs.t6-c1m2.large（1核2G）
   - 镜像：Alibaba Cloud Linux 3.2104 LTS
   - 系统盘：40GB ESSD

**步骤2：安全组配置**
```bash
# 在ECS控制台配置安全组规则
# 添加入站规则：
- 协议类型：自定义TCP
- 端口范围：3000
- 授权对象：0.0.0.0/0（允许所有IP访问）
- 协议类型：SSH（22端口）
```

**步骤3：部署项目**
```bash
# 连接ECS实例
ssh root@您的ECS公网IP

# 安装Node.js（阿里云镜像源）
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs

# 上传并部署项目
cd /home
mkdir project_11
# 上传文件后执行：
cd project_11
npm install
node server.js &
```

#### 方法B：阿里云容器服务

1. **创建镜像仓库**
   - 进入容器镜像服务 ACR
   - 创建命名空间和镜像仓库

2. **使用阿里云效进行CI/CD**
   - 创建流水线，自动构建和部署
   - 配置Git触发，代码提交自动部署

### 3. 其他云平台部署

#### Vercel部署（免费方案）
```bash
# 安装Vercel CLI
npm i -g vercel

# 登录Vercel
vercel login

# 部署到生产环境
vercel --prod

# 自定义域名（可选）
vercel domains add yourdomain.com
```

**优势：**
- 免费套餐足够使用
- 自动HTTPS
- 全球CDN加速
- 自动部署（GitHub集成）

#### Netlify部署
1. 将代码推送到GitHub仓库
2. 登录Netlify：https://app.netlify.com
3. 选择"New site from Git"
4. 连接GitHub仓库
5. 构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
6. 点击"Deploy site"

### 4. 域名绑定和HTTPS配置

#### 购买域名
- 腾讯云域名：https://dnspod.cloud.tencent.com
- 阿里云域名：https://wanwang.aliyun.com
- 推荐域名：gratitude-journey.xyz（约10元/年）

#### 配置DNS解析
```
# 添加A记录
主机记录：@ 或 www
记录类型：A
记录值：您的服务器IP
TTL：600秒
```

#### 配置HTTPS（免费证书）
```bash
# 使用Certbot获取免费SSL证书
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d yourdomain.com

# 或使用acme.sh（更轻量）
curl https://get.acme.sh | sh
acme.sh --issue -d yourdomain.com --webroot /var/www/html
```

## 🔧 服务器配置

### 环境要求
- Node.js >= 14.0.0
- npm >= 6.0.0
- 内存：至少512MB
- 磁盘空间：至少100MB

### 端口配置
- 默认端口：3000
- 可在环境变量中修改：`PORT=8080`

### 安全配置
```bash
# 使用非root用户运行
adduser gratitude
usermod -aG sudo gratitude
su - gratitude

# 配置防火墙
ufw allow 3000
ufw allow ssh
ufw enable
```

## 📱 移动端优化

网站已包含以下移动端优化：
- 响应式设计，适配各种屏幕尺寸
- 触摸事件支持
- 移动端专属CSS样式
- 图片懒加载优化
- 缓存策略优化

## 🔍 监控和维护

### 健康检查
```bash
# 检查服务状态
curl http://localhost:3000/health

# 预期响应
{
  "status": "OK",
  "timestamp": "2024-01-01T00:00:00.000Z",
  "uptime": 1234.56
}
```

### 日志查看
```bash
# 查看实时日志
tail -f server.log

# 或使用PM2查看
pm2 logs gratitude-journey
```

### 性能监控
```bash
# 安装监控工具
npm install -g clinic

# 性能分析
clinic doctor -- node server.js
```

## 🌐 域名配置

### Nginx反向代理配置
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### SSL证书配置（HTTPS）
```bash
# 使用Let's Encrypt获取免费证书
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d yourdomain.com
```

## 🎨 自定义调整

### 颜色主题
修改 `src/index.css` 中的渐变色：

```css
/* 主页背景色 */
.from-orange-50 via-amber-50 to-yellow-50

/* 各页面背景色 */
.from-amber-50 via-orange-50 to-yellow-50     /* 知遇之恩 */
.from-blue-50 via-indigo-50 to-purple-50      /* 启迪之光 */
.from-green-50 via-emerald-50 to-teal-50       /* 指路之师 */
.from-pink-50 via-purple-50 to-indigo-50      /* 成长之乐 */
```

### 动画速度
修改动画时长：

```css
.animate-fade-in {
  animation: fadeIn 0.8s ease-in-out; /* 调整这个数值 */
}
```

### 照片墙速度
在 `src/components/PhotoWall.tsx` 中调整：

```typescript
scrollAmount += 0.5; // 调整这个值控制滚动速度
```

## 📱 移动端优化

项目已针对移动端进行优化：
- 响应式布局
- 触摸友好的交互
- 适配小屏幕的导航
- 优化的字体大小

## 🔧 故障排除

### 音乐无法播放
1. 确保音频文件路径正确
2. 检查音频文件格式（支持MP3、WAV）
3. 现代浏览器要求用户交互后才允许播放音频

### 照片无法显示
1. 检查图片路径是否正确
2. 确保图片文件存在
3. 检查文件名大小写
4. 图片格式是否受支持（JPG、PNG、WebP）

### 页面切换不流畅
1. 检查网络连接
2. 清除浏览器缓存
3. 确保JavaScript正常加载

## 🌐 浏览器兼容性

支持以下现代浏览器：
- Chrome 88+
- Firefox 85+
- Safari 14+
- Edge 88+

## 📞 技术支持

如需技术支持或有任何问题，请参考：
1. `README_感恩之旅.md` - 详细项目说明
2. 项目源代码中的注释
3. Vite官方文档
4. React官方文档

---

**🎊 部署完成后，您将拥有一个温馨雅致的感恩主题网页，完美展现珍贵的回忆和真挚的情感！**